---
import { formatDate, type Lang } from '../i18n/ui';

interface Props {
	date: Date;
	lang: Lang;
	/** 카드처럼 좁은 곳은 short (Sep 12, 2026) */
	month?: 'long' | 'short';
}

const { date, lang, month } = Astro.props;
---

<time datetime={date.toISOString()}>{formatDate(date, lang, month)}</time>
